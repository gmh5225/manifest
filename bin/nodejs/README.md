place a single `node.exe` in this folder. shortcut provided to somewhat newer versions.
since NodeJS is not currently used (probably next-generation instead of CMD scripts),  
it is not needed right now. 