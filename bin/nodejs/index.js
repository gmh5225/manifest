"use strict";

/*
will be used, in the future, 
as an upgrade to CMD scripts, 
since NodeJS handles pipe-arguments better, 
has native support for UTF-8, 
and can be used more cools stuff such as Regular-Expression, 
without the pain of double escaping-characters and CMD syntax.

but for now CMD is fine.
*/